# Fill this file locally as backend/.env; never commit credentials.
EMAIL_USER=tekengyvan2@gmail.com
EMAIL_PASS=
# Gmail requires an app password; another provider can use the SMTP settings below.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
FRONTEND_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
PORT=5000
